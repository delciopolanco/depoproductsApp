export const THEMES = {
	LIGHT: 'LIGHT',
	DARK: 'DARK',
	NATURE: 'NATURE'
};

export const SIZES = {
	attachements: 500000
};

export const PATHS = {
	search: '/search',
	login: '/login'
};

export const API_URLS = {
	products: `${import.meta.env.VITE_SERVICE_BASE}/products`,
	execute: `${import.meta.env.VITE_SERVICE_BASE}/execute`
};

export const DATE_FORMATS = {
	default: 'dd/MM/yyyy'
};

export const TIME_FORMATS = {
	time12: 'hh:mm a'
};

export const CURRENCY_SYMBOLS = {
	pesos: '$'
};
