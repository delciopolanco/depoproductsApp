// ** Theme Type Import
import { Theme } from '@mui/material/styles';
import palette from '../settings/palette';

const MuiPalette = (theme: Theme) => {
	return palette;
};

export default MuiPalette;
